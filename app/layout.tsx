import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'TechConsult Pro - IT Consulting Solutions',
  description: 'Professional IT consulting services for modern businesses. Cloud solutions, digital transformation, and technology strategy.',
  keywords: 'IT consulting, cloud solutions, digital transformation, technology strategy',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  )
}

